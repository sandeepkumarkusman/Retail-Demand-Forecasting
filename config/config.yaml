# Default because xyzt_awesome is the only fully reproducible end-to-end pipeline
# in the analyzed notebooks; this does not imply it was the competition-winning solution.
active_solution: xyzt_awesome

# Future notebook-specific configuration belongs in isolated sections below.
# Do not combine settings between independent Kaggle solutions.
solutions:
  xyzt_awesome:
    data_dir: data/raw
    submission_path: outputs/submission.csv
  fourth_place_factor: {}
  polyfit_showcase: {}
  store_prediction: {}
  prophet_dumb_reference: {}
  blend_32_candidates: {}
  # These are deterministic project fallbacks, not notebook reproductions.
  # They use a historical store-item mean only because the original source
  # code/artifacts were unavailable. They must not be used for score claims.
  prophet_dumb_reference_fallback:
    data_dir: data/raw
    submission_path: outputs/submission.csv
  store_prediction_blend_fallback:
    data_dir: data/raw
    submission_path: outputs/submission.csv
  blend_32_candidates_fallback:
    data_dir: data/raw
    submission_path: outputs/submission.csv
